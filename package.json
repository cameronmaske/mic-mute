{
    "name": "audio",
    "version": "0.0.1",
    "description": "Simple bindings to list and mute window capture devices.",
    "main": "index.js",
    "private": true,
    "gypfile": true,
    "dependencies": {
        "bindings": "~1.2.1",
        "node-addon-api": "^1.0.0"
    },
    "scripts": {
        "build": "node-gyp rebuild",
        "test": "jest"
    },
    "devDependencies": {
        "jest": "^24.8.0",
        "mocha": "^10.2.0"
    }
}